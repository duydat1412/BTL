/**
 * Package chứa các Enum — hằng số có tên gọi ý nghĩa.
 *
 * <p>Enum giống như một danh sách cố định các giá trị.
 * Thay vì dùng String "OPEN", "RUNNING", "FINISHED" (dễ gõ sai),
 * ta dùng AuctionStatus.OPEN, AuctionStatus.RUNNING (IDE tự gợi ý).
 *
 * <p>Sẽ được Người B triển khai: AuctionStatus, UserRole, ItemType.
 */
package com.auction.common.enums;
