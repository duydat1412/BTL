/**
 * Package chứa DatabaseManager — quản lý kết nối database.
 *
 * <p>DatabaseManager sử dụng Singleton Pattern:
 * chỉ có DUY NHẤT 1 instance quản lý connection pool,
 * tránh tạo nhiều kết nối gây lãng phí tài nguyên.
 *
 * <p>Sẽ được triển khai ở Tuần 2 (phối hợp Người B).
 */
package com.auction.server.db;
