/**
 * Package chứa WebSocket handler — xử lý giao tiếp realtime.
 *
 * <p>WebSocket cho phép server CHỦ ĐỘNG gửi dữ liệu cho client,
 * không cần client hỏi lại. Ví dụ: khi có bid mới, tất cả
 * client đang xem phiên đấu giá đó đều nhận được thông báo ngay.
 *
 * <p>Sẽ được triển khai ở Tuần 4-5.
 */
package com.auction.server.websocket;
